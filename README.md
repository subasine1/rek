Anti-fraud scoring for mobile money in the UEMOA zone. Working monorepo.

    uv sync --all-packages    # install the eight packages
    make ci                   # lint, types, tests — exactly what CI runs
